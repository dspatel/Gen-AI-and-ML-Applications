# Module H — Momentum Filter (v1)

Momentum is used as a FILTER, not an entry strategy.

Pipeline:
- H1 builds daily allow/block decisions using EMA context
- H2 applies those decisions to Conservative ORB trades
